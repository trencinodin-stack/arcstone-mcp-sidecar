# NEXT EXPERIMENT

## Current state

The v0.1 architecture is frozen for implementation.

The next step is **not** MCP, WASM, networking, package publication, or generalized agent integration.

The next step is:

```text
compile
→ run local deterministic test suite
→ establish separate Windows producer/authority principals
→ execute T0–T17
→ preserve exact evidence
→ freeze Run 001
```

## Entry condition

Proceed only if:

```text
cargo test
```

passes on the target machine.

## Evidence target

The first frozen Execution Boundary evidence run should demonstrate the bounded v0.1 claim only.

A new scientific condition requires a new run identifier and, if it changes the research question, a new specification phase.
